#
# THIS FILE IS AUTOGENERATED; SEE "distros/generate.sh"!
#

FROM ubuntu:xenial

RUN apt-get update && apt-get install -y aufs-tools bash-completion btrfs-tools curl ca-certificates git iproute ipset iptables iputils-ping libcgroup-dev net-tools openssh-client openssh-server sudo systemd tar tree unzip vim-nox xz-utils gawk --no-install-recommends && rm -rf /var/lib/apt/lists/* \
&& ( \
cd /lib/systemd/system/sysinit.target.wants/; \
for i in *; do \
if [ "$i" != "systemd-tmpfiles-setup.service" ]; then \
rm -f $i; \
fi \
done \
) \
&& rm -f /lib/systemd/system/multi-user.target.wants/* \
&& rm -f /etc/systemd/system/*.wants/* \
&& rm -f /lib/systemd/system/local-fs.target.wants/* \
&& rm -f /lib/systemd/system/sockets.target.wants/*udev* \
&& rm -f /lib/systemd/system/sockets.target.wants/*initctl* \
&& rm -f /lib/systemd/system/anaconda.target.wants/* \
&& rm -f /lib/systemd/system/basic.target.wants/* \
&& rm -f /lib/systemd/system/graphical.target.wants/* \
&& ln -vf /lib/systemd/system/multi-user.target /lib/systemd/system/default.target
